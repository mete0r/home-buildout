[buildout]
extends = virtualenv.cfg
          venv25.cfg
parts += zc.buildout
         virtualenv
         venv25
         httpie
         pylama
         cookiecutter

[zc.buildout]
recipe = zc.recipe.egg
eggs = zc.buildout

[httpie]
recipe = zc.recipe.egg
eggs = httpie

[pylama]
recipe = zc.recipe.egg
eggs = pylama

[cookiecutter]
recipe = zc.recipe.egg
eggs = cookiecutter
