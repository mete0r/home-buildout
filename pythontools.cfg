[buildout]
extends = virtualenv.cfg
parts += zc.buildout
         virtualenv
         httpie
         pylama

[zc.buildout]
recipe = zc.recipe.egg
eggs = zc.buildout

[httpie]
recipe = zc.recipe.egg
eggs = httpie

[pylama]
recipe = zc.recipe.egg
eggs = pylama
